using Harmony12;

using UnityEngine;

namespace SXL.Main
{
    /*
    [HarmonyPatch(typeof(LevelManager), "FixBrokenShader")]
    internal class SXLPatches
    {
        static bool Prefix()
        {
            return !SXLRuntime.enabled;
        }
    }
    */
}
